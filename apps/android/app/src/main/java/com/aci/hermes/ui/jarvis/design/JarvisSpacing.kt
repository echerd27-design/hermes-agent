package com.aci.hermes.ui.jarvis.design

/**
 * Jarvis Prime spacing scale in dp.
 *
 * Pure Kotlin Ints. Downstream Compose call sites wrap as
 * `JarvisSpacing.md.dp`.
 */
object JarvisSpacing {
    const val xs: Int = 4
    const val sm: Int = 8
    const val md: Int = 12
    const val lg: Int = 16
    const val xl: Int = 24
}
