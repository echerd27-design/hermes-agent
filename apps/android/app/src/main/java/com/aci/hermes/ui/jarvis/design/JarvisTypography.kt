package com.aci.hermes.ui.jarvis.design

/**
 * Jarvis Prime type scale in sp.
 *
 * Pure Kotlin Ints + a font-family name string. A later wave wraps these
 * into a Compose Typography/TextStyle without changing names.
 */
object JarvisType {
    const val caption: Int = 12
    const val body: Int = 14
    const val title: Int = 18
    const val display: Int = 28

    const val FontFamilyName: String = "sans-serif-medium"
}
